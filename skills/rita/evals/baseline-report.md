# Baseline report

Status: no model-level baseline is claimed.

The evaluation contract was created before publication to make safety and operational assertions testable. A future benchmark must run the cases in `definition.json` against a no-skill agent, retain the prompts and sanitized outputs, and record pass/fail results before claiming quality uplift.
